filter(!is.na(key_1))|>
filter(!is.na(key_2))
unique_songs = data.frame("songs" = unique(c(pairs$song_key1, pairs$song_key2)))
View(unique_songs)
unique_songs = data.frame("song" = unique(c(pairs$song_key1, pairs$song_key2)))
test = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))
View(test)
test = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
filter(!is.na(key))
View(test)
View(pairs)
pairs_features <- pairs %>%
left_join(audio_data, by = c("song_key1" = "audio_key")) %>%
left_join(audio_data, by = c("song_key2" = "audio_key"))|>
filter(!is.na(key_1))|>
filter(!is.na(key_2))
View(pairs_features)
View(test)
test = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
filter(is.na(key))
unmatched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
filter(is.na(key))
View(unmatched)
unmatched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
filter(is.na(key))|>
select(song)
pairs_1 <- stringdist_left_join(
unmatched,
a1,
by = c("song" = "audio_key"),
method = "jw",        # Jaro-Winkler
max_dist = 0.10,
distance_col = "dist1"
)|>
filter(!is.na(key))
n = 5000
a1 = audio_data[1:n, ]
#a2 = audio_data[(n+1):nrow(audio_data), ]
pairs_1 <- stringdist_left_join(
unmatched,
a1,
by = c("song" = "audio_key"),
method = "jw",        # Jaro-Winkler
max_dist = 0.10,
distance_col = "dist1"
)|>
filter(!is.na(key))
View(pairs_1)
pairs_features <- pairs %>%
left_join(audio_data, by = c("song_key1" = "audio_key")) %>%
left_join(audio_data, by = c("song_key2" = "audio_key"), suffix = c("_1", "_2"))
left_join(pairs_1, by = c("song_key2" = "song"), suffix = c("_1", "_2"))
pairs_features <- pairs %>%
left_join(audio_data, by = c("song_key1" = "audio_key")) %>%
left_join(audio_data, by = c("song_key2" = "audio_key"),suffix = c("_1", "_2"))|>
left_join(pairs_1, by = c("song_key1" = "song"))|>
left_join(pairs_1, by = c("song_key2" = "song"), suffix = c("_1", "_2"))
pairs_features <- pairs %>%
left_join(audio_data, by = c("song_key1" = "audio_key")) %>%
left_join(audio_data, by = c("song_key2" = "audio_key"),suffix = c("_1", "_2"))|>
left_join(pairs_1, by = c("song_key1" = "song"))|>
left_join(pairs_1, by = c("song_key2" = "song"), suffix = c("_1", "_2"))|>
filter(!is.na(key_1))
pairs_features <- pairs %>%
left_join(audio_data, by = c("song_key1" = "audio_key")) %>%
left_join(audio_data, by = c("song_key2" = "audio_key"),suffix = c("_1", "_2"))|>
left_join(pairs_1, by = c("song_key1" = "song"))|>
left_join(pairs_1, by = c("song_key2" = "song"), suffix = c("_1", "_2"))|>
filter(!is.na(key_1))|>
filter(!is.na(key_2))
View(unmatched)
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
left_join(audio_data, by = c("song" = "song"))
View(unique_songs)
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
left_join(audio_data, by = "song")
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
left_join(pairs_1, by = c("song" = "song"))
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
left_join(pairs_1, by = c("song" = "song"))|>
filter(is.na(key))
View(matched)
matched = unique_songs|>
left_join(audio_data, by = c("song" = "audio_key"))|>
left_join(pairs_1, by = c("song" = "song"))|>
filter(is.na(key.x))
match_table <- data.frame(
song = character(),
audio_key = character(),
stringsAsFactors = FALSE
)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
# pairs <- pairs %>%
#   mutate(
#     artist_track_1 = clean(paste(artist_name1, track_name1)),
#     artist_track_2 = clean(paste(artist_name2, track_name2))
#   )
clean <- function(x) {
x %>%
tolower() %>%
str_remove_all("\\[.*?\\]") %>%
str_remove_all("\\(.*?\\)") %>%
str_replace_all("[^a-z0-9 ]", " ") %>%
str_squish()
}
pairs = read.csv("pairs.csv")
audio_data = read.csv("SpotifyAudioFeaturesApril2019.csv")|>
select(-track_id)|>
mutate(
audio_key = clean(paste(artist_name, track_name))
)
unique_songs = data.frame("song" = unique(c(pairs$song_key1, pairs$song_key2)))
match_table <- data.frame(
song = character(),
audio_key = character(),
stringsAsFactors = FALSE
)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
View(audio_data)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key"))
View(exact)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key"))%>%
filter(!is.na(song)) %>%
select(song)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key"))
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key"))%>%
filter(!is.na(key))
View(exact)
exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(key)) %>%   # or any guaranteed column from audio_data
transmute(
song = song,
audio_key = audio_key
)
View(match_table)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)|>
filter(!is.na(audio_key))
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)|>
filter(!is.na(audio_key))
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)|>
match_table <- bind_rows(match_table, exact)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(audio_key))|>
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(key))|>
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)
match_table <- bind_rows(match_table, exact)
View(match_table)
songs_remaining <- unique_songs
songs_remaining %>%
anti_join(match_table, by = "song")
songs_remaining = songs_remaining %>%
anti_join(match_table, by = "song")
unique_songs <- data.frame(
song = unique(c(pairs$song_key1, pairs$song_key2)),
stringsAsFactors = FALSE
)
songs_remaining <- unique_songs
songs_remaining = songs_remaining %>%
anti_join(match_table, by = "song")
# -----------------------------
# 1. LOOP: match songs
# -----------------------------
step = 5000
current = step + 1
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (nrow(fuzzy) == 0) break
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
# -----------------------------
# 2. FINAL CLEANUP
# -----------------------------
match_table <- match_table %>%
distinct(song, .keep_all = TRUE)
View(exact)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(key))|>
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)|>
distinct(song)
View(exact)
exact <- exact <- unique_songs %>%
left_join(audio_data, by = c("song" = "audio_key")) %>%
filter(!is.na(key))|>
mutate(audio_key = song) %>%   # enforce identity mapping
select(song, audio_key)|>
distinct(song, audio_key)
View(exact)
match_table <- bind_rows(match_table, exact)
match_table <- bind_rows(match_table, exact)|>%>%
match_table <- bind_rows(match_table, exact)%>%
distinct(song, .keep_all = TRUE)
unique_songs <- data.frame(
song = unique(c(pairs$song_key1, pairs$song_key2)),
stringsAsFactors = FALSE
)
songs_remaining <- unique_songs
songs_remaining = songs_remaining %>%
anti_join(match_table, by = "song")
step = 5000
current = step + 1
step = 1000
current = step + 1
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (nrow(fuzzy) == 0) break
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
cat("matched", nrow(fuzzy), "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
cat("matched", nrow(fuzzy),"Current:", current "\n")
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current > nrow(audio_data)) current = nrow(audio_data)
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "audio_key")
}
# -----------------------------
# 1. LOOP: match songs
# -----------------------------
step = 500
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current > nrow(audio_data)) current = nrow(audio_data)
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "audio_key")
}
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current >= nrow(audio_data)) break
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "audio_key")
}
View(match_table)
View(songs_remaining)
View(songs_remaining)
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current >= nrow(audio_data)) break
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = c("song" = "audio_key"))
}
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current >= nrow(audio_data)) break
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
match_table <- match_table %>%
distinct(song, .keep_all = TRUE)
repeat {
cat("Remaining songs:", nrow(songs_remaining), "\n")
if (nrow(songs_remaining) == 0) break
# -------------------------
# B. FUZZY MATCH (ONLY UNMATCHED)
# -------------------------
fuzzy <- stringdist_left_join(
songs_remaining,
audio_data[current-step:current,],
by = c("song" = "audio_key"),
method = "jw",
max_dist = 0.10
) %>%
filter(!is.na(audio_key)) %>%
select(song, audio_key)
current = current + step
if (current >= nrow(audio_data)) break
cat("matched", nrow(fuzzy),"Current:", current, "\n")
# add fuzzy matches
match_table <- bind_rows(match_table, fuzzy) %>%
distinct(song, .keep_all = TRUE)
# update remaining again
songs_remaining <- songs_remaining %>%
anti_join(match_table, by = "song")
}
write.csv(match_table, "match_table.csv")
save.image("~/Sebastian Cruz/Middlebury/Spring 2026/CS451 Machine Learning/BeatNet/.RData")
